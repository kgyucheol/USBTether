#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        systemctl stop phone-socks.service >/dev/null 2>&1 || true
        # 방화벽 규칙이 남으면 인터넷이 끊긴 채로 남는다. 반드시 지운다.
        nft delete table inet phonesocks >/dev/null 2>&1 || true
        ;;
esac

exit 0
